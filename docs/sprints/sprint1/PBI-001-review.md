# PBI-001 コードレビュー結果

## 比較対象
`git diff develop...HEAD`（71ファイル変更、5866挿入/1795削除）

## 総合判定
APPROVED（指摘事項対応済み）

---

## P1 - High

### 1. DB_HOST が host.docker.internal にハードコード
- **判断**: 対応不要
- **理由**: `infra/package.json` の `local-api` スクリプトでの利用前提。Windows Docker Desktop 環境での動作を意図した設定であり、問題なし。

### 2. createVillage ハンドラーで認証チェックがバリデーション後
- **判断**: 修正済み
- **対応内容**: 認証チェック（`X-User-Id` ヘッダー確認）をJSONパースの前に移動

---

## P2 - Medium

### 3. date-fns 依存が未使用
- **判断**: 対応不要
- **理由**: `formattedDate` 機能での使用を予定しており、意図的に追加済み。

### 4. CDKスタック内の execSync('npm install') 副作用
- **判断**: 修正済み
- **対応内容**: `cdk diff` / `cdk synth` のたびに副作用が走らないよう改善

### 5. E2EテストのモックID管理（並列実行リスク）
- **判断**: 修正済み
- **対応内容**: テスト間の共有状態を解消

---

## P3 - Low

### 6. form.value! の非null アサーション
- **判断**: 修正済み
- **対応内容**: オプショナルチェーンまたはガード処理を追加

### 7. .githooks/commit-msg 末尾改行なし
- **判断**: 修正済み（ユーザー対応済み）

### 8. apps/frontend/test-results/.last-run.json がコミットに含まれている
- **判断**: 修正済み
- **対応内容**: ファイルを削除し .gitignore に追加

### 9. 「詳細を見る」リンク先が未実装ルート
- **判断**: 修正済み
- **対応内容**: 「詳細を見る」ボタン/リンクを削除

---

## PBI適合確認

| Scenario | 判定 | 備考 |
|---|---|---|
| Scenario 1: 正常に村を作成できる | OK | E2E確認済み |
| Scenario 2: 同名の村を複数作成できる | OK | E2E・統合テスト確認済み |
| Scenario 3: 村名が空の場合は作成できない | OK | E2E確認済み |
| Scenario 4: 村名が128文字を超える場合は作成できない | OK（仕様変更） | maxlength属性でブロック。128文字MAXの村名は実用上めったに入力されないため、エラーメッセージではなくブロックで対応。PBI・設計ドキュメントに反映済み。 |
| Scenario 5: 他ユーザーの村は編集できない | OK | 一覧の表示分離確認済み。編集機能は後続PBIで実装予定 |
| Scenario 6: 村一覧には自分の村のみ表示される | OK | E2E確認済み |
