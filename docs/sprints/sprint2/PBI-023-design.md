# PBI-023 設計ドキュメント — screen/home.md を「仕様の合意記録」に再編する

Sprint 2 / 作成日: 2026-05-31

**想定読者**: 実装担当者  
**目的**: PBI-022 と同一ルールを `docs/design/detail/screen/home.md` に適用するための変換仕様。  
**スコープ**: ドキュメントのみの改訂。実装コード・ロジックの変更は含まない。

---

## 変換対象ファイル

`docs/design/detail/screen/home.md` の 1 ファイルのみ。

---

## 変換内容

### 削除するもの

| 種類 | 対象 |
|---|---|
| コンポーネント名含む見出し | `## トップページ（\`HomeView.vue\`）` の括弧部分 |
| コンポーネント名含む見出し | `## アプリケーションレイアウト（\`App.vue\`）` の括弧部分 |
| ASCII コンポーネントツリー | `### コンポーネント構成`（`HomeView.vue` 構成）のコードブロック |
| TypeScript コードブロック | `### 認証状態の判定` の `const userId = ref(...)` コードブロック |
| TypeScript コードブロック | `### 「始める」ボタンの処理` の `function start()` コードブロック |
| ASCII コンポーネントツリー | `## アプリケーションレイアウト > ### 構成` のコードブロック |

### 維持・変換するもの

| 種類 | 処置 |
|---|---|
| 認証状態の判定ロジック | コードを削除し、「ローカルストレージのユーザー ID 有無で表示を切り替える」という振る舞いを箇条書きで残す |
| 「始める」ボタンの処理 | コードを削除し、「ローカルストレージに保存して村一覧に遷移する」という手順を文章で残す |
| バリデーション方針の判断理由 | 「シンプルなボタン制御で十分のためフォームバリデーションは使用しない」という意図を維持 |
| ナビゲーションの振る舞い | 未ログイン時のリダイレクト方針を維持 |
| 変更履歴 | PBI-023 の追記 |

---

## 受け入れ確認チェックリスト

- [ ] `HomeView.vue` や `App.vue` がセクション見出しに登場しない
- [ ] TypeScript のコードブロックが登場しない
- [ ] ASCII コンポーネントツリーのコードブロックが登場しない
- [ ] 認証状態の判定・「始める」ボタン処理・バリデーション方針の理由が文章で読み取れる

---

## 変更履歴

| PBI | 変更日 | 変更内容 |
|---|---|---|
| PBI-023 | 2026-05-31 | 新規作成 |
