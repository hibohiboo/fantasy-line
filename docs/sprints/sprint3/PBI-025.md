# PBI-025 作業計画: MCP サーバー（Serena / context7）削除

## 概要

ADR-001・ADR-002 の決定に基づき、`.claude/settings.json` から `serena` と `context7` のエントリを削除する。

## 変更ファイル

| ファイル | 変更内容 |
|---|---|
| `.claude/settings.json` | `serena`・`context7` エントリを削除 |

## 依存関係・前提条件

- ADR-001・ADR-002 の承認済み（Phase 2 完了）
- 他の MCP（`azure-devops`・`playwright`・`aws-iac`）への影響なし

## SubAgent 割り当て

| サブタスク | 担当 | 備考 |
|---|---|---|
| settings.json 変更 | Orchestrator 直接実施 | 設定ファイル変更のみ。SubAgent 不要 |

## 完了条件チェックリスト

- [ ] `.claude/settings.json` から `serena` エントリが削除されている
- [ ] `.claude/settings.json` から `context7` エントリが削除されている
- [ ] `azure-devops`・`azure-devops-mcp`・`playwright`・`aws-iac` は残存している
- [ ] JSON が構文エラーなく整形されている
- [ ] ADR-001・ADR-002 が `docs/adr/` に存在する
