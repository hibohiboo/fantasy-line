# 実装計画 — PBI-001 村を作成する

作成日: 2026-05-05

## 関連ドキュメント

- [PBI](../pbi/01-village-management/PBI-001.md)
- [基本設計](../design/basic/village.md)
- [詳細設計](../design/detail/village.md)
- [データモデル](../design/data-model/village.md)
- [スプリントノート](../sprints/sprint1/PBI-001.md)

---

## 実装方針

- TDD（Red → Green → Refactor）の順序で実装する
- 依存関係の順序: スキーマ → DBスキーマ → マイグレーション → APIハンドラー → フロントエンド
- 認証は X-User-Id ヘッダーによるモック認証（PBI-014 未実装のため）
- 1チェックリスト項目 = 1コミットを目安にする
- **テスト方針**: テストは責務が生まれたレイヤーで1回だけ行う。上位レイヤーで下位レイヤーのロジックを再テストしない（詳細: [testing.md](../design/non-functional/testing.md)）
- **TDD の Red フェーズ**: "ファイルが存在しない" ことで失敗させるのは不適切。必ずスタブハンドラー（固定値を返す最小実装）を先に作成し、「振る舞いが間違い」で失敗させること
- **レスポンスのスキーマ検証**: テストでレスポンスボディをパースする際は `CreateVillageResponseSchema.parse()` / `ListVillagesResponseSchema.parse()` を使う。フィールドを個別にアサートするだけでなくスキーマ全体でパースすることで、フロントエンドとのスキーマ差異を型レベルで防ぐ（`echo.small.test.ts` の EchoResponseSchema パターン参照）

---

## チェックリスト

### Phase 1: 共有スキーマ（packages/schema）

- [x] テストコード作成（Red）: `packages/schema/src/village.test.ts` — CreateVillageSchema のバリデーションテスト（空文字・128文字・129文字・正常ケース）
- [x] 実装（Green）: `packages/schema/src/village.ts` — CreateVillageSchema / VillageSchema / VillageResponseSchema（API レスポンス用・createdAt: z.string()）/ CreateVillageResponseSchema / ListVillagesResponseSchema と各型を新規作成（OpenAPI の Village.createdAt: string/date-time に合わせ DB 用と API レスポンス用を分離）
- [x] 実装（Green）: `packages/schema/src/index.ts` — village.ts の export を追加

---

### Phase 2: DBスキーマ・マイグレーション（apps/api）

- [x] 実装（Green）: `apps/api/src/db/schema.ts` — villages テーブル定義（id, name, owner_id, created_at）を追加
- [x] マイグレーション: `apps/api/drizzle/0001_breezy_iron_patriot.sql` — `drizzle-kit generate` で villages テーブルの CREATE 文を生成・レビュー済み

---

### Phase 3: createVillage ハンドラー（apps/api）

- [x] スタブ作成: `apps/api/src/handlers/createVillage.ts` — 固定値（200）を返す最小スタブを作成する（ファイル不在でのエラーではなく振る舞いの失敗で Red にするため）
- [x] テストコード作成（Red）: `apps/api/tests/handlers/createVillage.small.test.ts` — ハンドラー固有のケースを DB モックで確認（JSONパース失敗→400・バリデーションエラー代表1ケース→400・未認証→401）。バリデーション詳細は Phase 1 のスキーマテストで完結しているため繰り返さない
- [x] 実装（Green）: `apps/api/src/handlers/createVillage.ts` — POST /villages ハンドラー（JSON パース → Zod バリデーション → X-User-Id ヘッダーから owner_id 取得 → INSERT → SELECT → 201 返却・構造化ログ出力）
- [x] テストコード作成（Red→Green）: `apps/api/tests/handlers/createVillage.medium.test.ts` — Testcontainers MySQL を使用した正常系・異常系の統合テスト（201作成・400バリデーション・401未認証）
- [x] リファクタリング: `apps/api/tests/handlers/createVillage.medium.test.ts` — 正常系レスポンスのパースを `CreateVillageResponseSchema.parse()` に変更してフロントエンドとのスキーマ差異を防ぐ
- [x] リファクタリング: `apps/api/src/http.ts` 新規作成 + `apps/api/src/handlers/createVillage.ts` — `json(statusCode, data)` ヘルパーを導入して statusCode/headers/body の重複を除去

---

### Phase 4: listVillages ハンドラー（apps/api）

- [x] スタブ作成: `apps/api/src/handlers/listVillages.ts` — 固定値（200）を返す最小スタブを作成する
- [x] テストコード作成（Red）: `apps/api/tests/handlers/listVillages.small.test.ts` — 未認証ケース（401）を DB モックで確認
- [x] 実装（Green）: `apps/api/src/handlers/listVillages.ts` — GET /villages ハンドラー（X-User-Id ヘッダーから owner_id 取得 → owner_id 条件で SELECT → 200 返却・構造化ログ出力）
- [x] テストコード作成（Red→Green）: `apps/api/tests/handlers/listVillages.medium.test.ts` — Testcontainers MySQL を使用した統合テスト（空配列・自分の村のみ返却・他ユーザーの村を含まない）+ `ListVillagesResponseSchema.parse()` でスキーマ検証済み
- [x] リファクタリング: `apps/api/src/handlers/listVillages.ts` — クエリに `ORDER BY created_at DESC` を追加（画面設計でソートは API 側と決定。詳細: [画面設計](../../design/screen/village.md)）
- [x] テスト更新: `apps/api/tests/handlers/listVillages.medium.test.ts` — ソート順（最新作成順）の確認ケースを追加
- [x] ドキュメント更新: `docs/design/openapi/openapi.yaml` — GET /villages の description にソート順を追記・example を降順に修正

---

### Phase 4.5: API 統合テスト（apps/api）

createVillage・listVillages の2ハンドラーを組み合わせた DB ありの統合テスト。
「API として正しく連携できているか」を確認する。ユーザー操作レベルの受け入れは Phase 9.5 の E2E で行う。

- [x] テストコード作成（Red→Green）: `apps/api/tests/integration/village.integration.test.ts` — 以下のシナリオを Testcontainers MySQL で確認する

  | 確認内容 |
  |---|
  | POST /villages → 201・villages テーブルに owner_id 付きで保存・レスポンスに id/name/ownerId/createdAt が含まれる |
  | 同じ name で2回 POST → 両方 201・DB に2件存在する |
  | POST /villages のリクエストボディに owner_id を含めても認証ヘッダーの値で上書きされる |
  | ユーザーA・Bそれぞれ村を作成後、GET /villages → 各ユーザーは自分の村のみ取得できる |

---

### Phase 5: インフラ（infra）

- [x] 実装（Green）: `infra/lib/infra-stack.ts` — createVillage Lambda 関数定義・listVillages Lambda 関数定義・API Gateway /villages リソースへの POST / GET メソッド追加

---

### Phase 6: フロントエンド — Vuetify セットアップ（apps/frontend）

- [x] 実装（Green）: `apps/frontend/package.json` — vuetify（dependencies）・vite-plugin-vuetify（devDependencies）を追加（手動インストール）
- [x] 実装（Green）: `apps/frontend/vite.config.ts` — vite-plugin-vuetify を plugins に追加（autoImport: true）
- [x] 実装（Green）: `apps/frontend/src/main.ts` — createVuetify でアースカラーテーマを設定し `app.use(vuetify)` を追加
- [x] 実装（Green）: `apps/frontend/src/assets/main.css` — Cinzel フォントの import と h1〜h3 へのフォント適用を追加

---

### Phase 7: フロントエンド — Pinia ストア（apps/frontend）

- [x] テストコード作成（Red）: `apps/frontend/src/stores/__tests__/village.small.spec.ts` — fetchVillages・createVillage アクションの動作確認（fetch モック使用）※ファイル名は Small テストとして命名
- [x] 実装（Green）: `apps/frontend/src/stores/village.ts` — villages / isLoading / error ステート・fetchVillages アクション（GET /villages）・createVillage アクション（POST /villages）を新規作成

---

### Phase 8: フロントエンド — コンポーネント・View（apps/frontend）

- [x] テストコード作成（Red）: `apps/frontend/src/components/village/__tests__/VillageCard.small.spec.ts` — VillageCard に村名・作成日時（yyyy-MM-dd HH:mm:ss JST）が表示されることを確認
- [x] 実装（Green）: `apps/frontend/src/components/village/VillageCard.vue` — 村1件を表示する Vuetify v-card コンポーネント（village props: VillageResponse）。日時は `Intl.DateTimeFormat` + `formatToParts` で JST 変換
- [x] テストコード作成（Red）: `apps/frontend/src/views/__tests__/VillageListView.medium.spec.ts` — 村一覧の表示・「村を作成」ボタン存在・ローディング表示・空状態の表示・エラー表示を確認
- [x] 実装（Green）: `apps/frontend/src/views/VillageListView.vue` — 村一覧ページ（v-container + v-row + VillageCard グリッド・v-empty-state・「村を作成」ボタン）
- [x] テストコード作成（Red）: `apps/frontend/src/views/__tests__/VillageCreateView.medium.spec.ts` — フォーム表示・空文字バリデーション・128文字超バリデーション・送信成功後のリダイレクトを確認
- [x] 実装（Green）: `apps/frontend/src/views/VillageCreateView.vue` — 村作成フォームページ（v-text-field + `CreateVillageSchema.shape.name.safeParse()` によるスキーマ駆動バリデーション + v-btn :loading + v-alert エラー表示）
- [x] 追加: `apps/frontend/vitest.setup.ts` — ResizeObserver モック（jsdom 対応）
- [x] 追加: `apps/frontend/vitest.config.ts` — setupFiles・server.deps.inline（vuetify）を追加

---

### Phase 9: フロントエンド — ルーター（apps/frontend）

- [ ] 実装（Green）: `apps/frontend/src/router/index.ts` — /villages → VillageListView・/villages/new → VillageCreateView のルートを追加（モック認証ガードを設定）

---

### Phase 9.5: E2E 受け入れテスト（apps/frontend）

PBI の Gherkin シナリオに 1 対 1 で対応するブラウザテスト。
フロントエンド・API・DB がすべて揃った状態で「ユーザーが実際に村を作成できる」ことを確認する。
これが全件 GREEN になって初めて PBI の受け入れ条件を満たしたとみなす。

- [ ] セットアップ: `apps/frontend` — Playwright をインストールし `playwright.config.ts` を設定する（`npm install -D @playwright/test`・dev サーバー起動設定）
- [ ] テストコード作成（Red）: `apps/frontend/e2e/village.spec.ts` — Gherkin シナリオに 1 対 1 で対応する E2E テストを記述する

  | Gherkin シナリオ | E2E での確認内容 |
  |---|---|
  | Scenario 1: 正常に村を作成できる | /villages/new で村名入力 → 作成 → /villages に遷移して一覧に表示される |
  | Scenario 2: 同名の村を複数作成できる | 同じ村名で2回作成 → 一覧に2件表示される |
  | Scenario 3: 村名が空の場合は作成できない | 村名空のまま作成ボタン押下 → バリデーションエラーが表示される・画面遷移しない |
  | Scenario 4: 村名が128文字を超える場合は作成できない | `maxlength="128"` によりブラウザが129文字目の入力をブロックすることを確認（入力値の長さ ≤ 128 になること） ※バリデーションエラー表示の確認は行わない（理由は下記注記参照） |
  | Scenario 5: 他ユーザーの村は編集できない | ユーザーAの村一覧にユーザーBの村が表示されない（Scenario 6 と一体で確認） |
  | Scenario 6: 村一覧には自分の村のみ表示される | ユーザーA・B それぞれでログインして一覧を確認 → 自分の村のみ表示される |

  **Scenario 4 注記**: 「129文字を入力してバリデーションエラーを確認」は採用しない。理由：
  - バックエンドの Zod スキーマテスト（packages/schema）が 128/129 文字の境界値を担保している
  - `maxlength="128"` により UI 上ユーザーは 129 文字目を入力できない（入力不可 = 超過不可）
  - テスト内部で HTML 制約を無効化して強制入力することは「実際のユーザー操作」を逸脱し、E2E テストの意義に反する

- [ ] 実装調整（Green）: E2E テスト通過に必要なフロントエンド修正があれば対応する

---

### Phase 10: lint・型チェック・最終確認

- [ ] lint・型チェック: `apps/api` — `npm run lint` を実行してエラーがないことを確認
- [ ] lint・型チェック: `packages/schema` — `npm run lint` を実行してエラーがないことを確認
- [ ] lint・型チェック: `apps/frontend` — `npm run lint` を実行してエラーがないことを確認
- [ ] ビルド確認: `apps/frontend` — `npm run build` を実行してビルドエラーがないことを確認

---

## 依存関係メモ

| フェーズ | 前提 |
|---|---|
| Phase 1（スキーマ） | なし（起点） |
| Phase 2（DB スキーマ・マイグレーション） | Phase 1 完了後 |
| Phase 3（createVillage ハンドラー） | Phase 1・Phase 2 完了後 |
| Phase 4（listVillages ハンドラー） | Phase 1・Phase 2 完了後 |
| Phase 4.5（API 統合テスト） | Phase 3・Phase 4 完了後 |
| Phase 5（インフラ） | Phase 4.5 完了後 |
| Phase 6（Vuetify セットアップ） | なし（フロントエンド起点） |
| Phase 7（Pinia ストア） | Phase 1・Phase 6 完了後 |
| Phase 8（コンポーネント・View） | Phase 7 完了後 |
| Phase 9（ルーター） | Phase 8 完了後 |
| Phase 9.5（E2E 受け入れテスト） | Phase 9・Phase 5（インフラ）完了後 |
| Phase 10（lint・最終確認） | 全 Phase 完了後 |

## 認証モック方針

- `X-User-Id` リクエストヘッダーの値を `owner_id` として使用する
- ヘッダーが存在しない場合は 401 Unauthorized を返す
- テストでは `headers: { 'X-User-Id': 'user-123' }` を設定して動作確認する

## 未解決事項

- 村名がスペースのみの場合の許可可否は未確定（PBI-001 不明点より）
  - 現時点では Zod の `.min(1)` により空文字のみ弾く（スペースのみは通過する）実装とし、仕様確定後に調整する
