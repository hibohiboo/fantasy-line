# 実装計画 — PBI-001 村を作成する

作成日: 2026-05-05

## 関連ドキュメント

- [PBI](../pbi/01-village-management/PBI-001.md)
- [基本設計](../design/basic/village.md)
- [詳細設計](../design/detail/village.md)
- [データモデル](../design/data-model/village.md)
- [スプリントノート](../sprints/sprint1/PBI-001.md)

---

## 実装方針

- TDD（Red → Green → Refactor）の順序で実装する
- 依存関係の順序: スキーマ → DBスキーマ → マイグレーション → APIハンドラー → フロントエンド
- 認証は X-User-Id ヘッダーによるモック認証（PBI-014 未実装のため）
- 1チェックリスト項目 = 1コミットを目安にする

---

## チェックリスト

### Phase 1: 共有スキーマ（packages/schema）

- [ ] テストコード作成（Red）: `packages/schema/src/village.test.ts` — CreateVillageSchema のバリデーションテスト（空文字・128文字・129文字・正常ケース）
- [ ] 実装（Green）: `packages/schema/src/village.ts` — CreateVillageSchema / VillageSchema / CreateVillageResponseSchema / ListVillagesResponseSchema と各型を新規作成
- [ ] 実装（Green）: `packages/schema/src/index.ts` — village.ts の export を追加

---

### Phase 2: DBスキーマ・マイグレーション（apps/api）

- [ ] 実装（Green）: `apps/api/src/db/schema.ts` — villages テーブル定義（id, name, owner_id, created_at）を追加
- [ ] マイグレーション: `apps/api/drizzle/0001_*.sql` — `npx drizzle-kit generate` で villages テーブルの CREATE 文を生成し、生成 SQL をレビューしてコミット

---

### Phase 3: createVillage ハンドラー（apps/api）

- [ ] テストコード作成（Red）: `apps/api/tests/handlers/createVillage.small.test.ts` — バリデーションエラーケース（JSONパース失敗・name未指定・name空文字・name129文字・未認証）を DB モックで確認
- [ ] 実装（Green）: `apps/api/src/handlers/createVillage.ts` — POST /villages ハンドラー（JSON パース → Zod バリデーション → X-User-Id ヘッダーから owner_id 取得 → INSERT → SELECT → 201 返却・構造化ログ出力）
- [ ] テストコード作成（Red）: `apps/api/tests/handlers/createVillage.medium.test.ts` — Testcontainers MySQL を使用した正常系・異常系の統合テスト（201作成・400バリデーション・401未認証）
- [ ] リファクタリング: `apps/api/src/handlers/createVillage.ts` — テスト通過後にロジック整理（必要な場合）

---

### Phase 4: listVillages ハンドラー（apps/api）

- [ ] テストコード作成（Red）: `apps/api/tests/handlers/listVillages.small.test.ts` — 未認証ケース（401）を DB モックで確認
- [ ] 実装（Green）: `apps/api/src/handlers/listVillages.ts` — GET /villages ハンドラー（X-User-Id ヘッダーから owner_id 取得 → owner_id 条件で SELECT → 200 返却・構造化ログ出力）
- [ ] テストコード作成（Red）: `apps/api/tests/handlers/listVillages.medium.test.ts` — Testcontainers MySQL を使用した統合テスト（空配列・自分の村のみ返却・他ユーザーの村を含まない）
- [ ] リファクタリング: `apps/api/src/handlers/listVillages.ts` — テスト通過後にロジック整理（必要な場合）

---

### Phase 5: インフラ（infra）

- [ ] 実装（Green）: `infra/lib/infra-stack.ts` — createVillage Lambda 関数定義・listVillages Lambda 関数定義・API Gateway /villages リソースへの POST / GET メソッド追加

---

### Phase 6: フロントエンド — Vuetify セットアップ（apps/frontend）

- [ ] 実装（Green）: `apps/frontend/package.json` — vuetify・vite-plugin-vuetify を依存関係に追加（`npm install vuetify vite-plugin-vuetify`）
- [ ] 実装（Green）: `apps/frontend/vite.config.ts` — vite-plugin-vuetify を plugins に追加（autoImport: true）
- [ ] 実装（Green）: `apps/frontend/src/main.ts` — createVuetify でアースカラーテーマを設定し `app.use(vuetify)` を追加
- [ ] 実装（Green）: `apps/frontend/src/assets/main.css` — Cinzel フォントの import と h1〜h3 へのフォント適用を追加

---

### Phase 7: フロントエンド — Pinia ストア（apps/frontend）

- [ ] テストコード作成（Red）: `apps/frontend/src/stores/__tests__/village.spec.ts` — fetchVillages・createVillage アクションの動作確認（API モックを使用）
- [ ] 実装（Green）: `apps/frontend/src/stores/village.ts` — villages / isLoading / error ステート・fetchVillages アクション（GET /villages）・createVillage アクション（POST /villages）を新規作成

---

### Phase 8: フロントエンド — コンポーネント・View（apps/frontend）

- [ ] テストコード作成（Red）: `apps/frontend/src/components/village/__tests__/VillageCard.spec.ts` — VillageCard に村名・作成日が表示されることを確認
- [ ] 実装（Green）: `apps/frontend/src/components/village/VillageCard.vue` — 村1件を表示する Vuetify v-card コンポーネント（village props: Village）
- [ ] テストコード作成（Red）: `apps/frontend/src/views/__tests__/VillageListView.spec.ts` — 村一覧の表示・「村を作成」ボタン存在・ローディング表示・空状態の表示を確認
- [ ] 実装（Green）: `apps/frontend/src/views/VillageListView.vue` — 村一覧ページ（v-container + v-row + VillageCard グリッド・v-empty-state・「村を作成」ボタン）
- [ ] テストコード作成（Red）: `apps/frontend/src/views/__tests__/VillageCreateView.spec.ts` — フォーム表示・空文字バリデーション・128文字超バリデーション・送信成功後のリダイレクトを確認
- [ ] 実装（Green）: `apps/frontend/src/views/VillageCreateView.vue` — 村作成フォームページ（v-text-field + :rules バリデーション + v-btn :loading + v-alert エラー表示）

---

### Phase 9: フロントエンド — ルーター（apps/frontend）

- [ ] 実装（Green）: `apps/frontend/src/router/index.ts` — /villages → VillageListView・/villages/new → VillageCreateView のルートを追加（モック認証ガードを設定）

---

### Phase 10: lint・型チェック・最終確認

- [ ] lint・型チェック: `apps/api` — `npm run lint` を実行してエラーがないことを確認
- [ ] lint・型チェック: `packages/schema` — `npm run lint` を実行してエラーがないことを確認
- [ ] lint・型チェック: `apps/frontend` — `npm run lint` を実行してエラーがないことを確認
- [ ] ビルド確認: `apps/frontend` — `npm run build` を実行してビルドエラーがないことを確認

---

## 依存関係メモ

| フェーズ | 前提 |
|---|---|
| Phase 1（スキーマ） | なし（起点） |
| Phase 2（DB スキーマ・マイグレーション） | Phase 1 完了後 |
| Phase 3（createVillage ハンドラー） | Phase 1・Phase 2 完了後 |
| Phase 4（listVillages ハンドラー） | Phase 1・Phase 2 完了後 |
| Phase 5（インフラ） | Phase 3・Phase 4 完了後 |
| Phase 6（Vuetify セットアップ） | なし（フロントエンド起点） |
| Phase 7（Pinia ストア） | Phase 1・Phase 6 完了後 |
| Phase 8（コンポーネント・View） | Phase 7 完了後 |
| Phase 9（ルーター） | Phase 8 完了後 |
| Phase 10（lint・最終確認） | 全 Phase 完了後 |

## 認証モック方針

- `X-User-Id` リクエストヘッダーの値を `owner_id` として使用する
- ヘッダーが存在しない場合は 401 Unauthorized を返す
- テストでは `headers: { 'X-User-Id': 'user-123' }` を設定して動作確認する

## 未解決事項

- 村名がスペースのみの場合の許可可否は未確定（PBI-001 不明点より）
  - 現時点では Zod の `.min(1)` により空文字のみ弾く（スペースのみは通過する）実装とし、仕様確定後に調整する
