{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "lint": "eslint . --fix",
    "ncu": "ncu -u"
  },
  "dependencies": {
    "@repo/schema": "*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "eslint": "^10.2.1"
  }
}
